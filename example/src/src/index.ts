import {
  setStorageType,
  createStore,
  StateMachineProvider,
  useStateMachine,
  StateMachineContext,
  // @ts-ignore
  setStorageName,
} from './stateMachine';
import DevTool from './components/DevTool';

export {
  setStorageType,
  createStore,
  StateMachineProvider,
  useStateMachine,
  StateMachineContext,
  // @ts-ignore
  setStorageName,
  DevTool,
};
